import React from "react";
import "../App.css";

export var Contact = (props) => {
  return (
    <div
      className="contact"
      style={{
        fontSize: 200,
        color: "lime",
        display: "flex",
        width: "100vw",
        alignItems: "center",
        justifyContent: "center",
      }}
    >
      hmu
    </div>
  );
};
